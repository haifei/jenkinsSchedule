package jenkins.util.login;

import org.junit.Test;

/**
 * Created by wanghf on 2018/9/12.
 */
public class HttpUtilTest {

    @Test
    public  void testUserAuth(){
       /* try {
            org.apache.http.HttpResponse   response=HttpUtils.postUserAuth(Contents.USER_AUTH_CAS_HOST+Contents.USER_AUTH__CAS_URL,"wanghf","#WHFwhf8244");

            String responseText = HttpUtils.getResponseText(response);
            if (response != null && response.getStatusLine().getStatusCode() == 200 && responseText != null) {
                JSONObject dataObj = JSONObject.fromObject(responseText);

                if (null != dataObj) {

                }
            }
        } catch (UnsupportedEncodingException e) {
            e.printStackTrace();
        }*/



    }
}
