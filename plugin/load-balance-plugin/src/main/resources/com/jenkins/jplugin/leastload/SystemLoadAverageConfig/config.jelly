<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:section title="${%System Load Average}">
        <f:entry title="${Threshold}" field="averageThreshold">
            <f:textbox default="${it.averageThreshold}"/>
        </f:entry>
    </f:section>
</j:jelly>